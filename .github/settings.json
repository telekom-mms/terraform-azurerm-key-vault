{
  "repos": {
    "description": "A Terraform module that manages the key-vault resources from the azurerm provider.",
    "visibility": "public",
    "default_branch": "main",
    "topics": [
      "terraform",
      "azure",
      "key-vault",
      "key-vault-secret"
    ]
  }
}
